import Home from '@/components/Home';

export default function Index({ pageTransition, pageVariants }) {

	return (
		<>
			<Home pageTransition={pageTransition} variants={pageVariants}/>
		</>
	);
}
  